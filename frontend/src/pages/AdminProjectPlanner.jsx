import { useEffect, useState } from "react";
import toast from "react-hot-toast";
import { createProject, deleteProject, getDepartments, getManagers, getProjects, updateProject } from "../services/projectService";

const blank = { project_name: "", description: "", manager_id: "", department_id: "", start_date: "", end_date: "", status: "Active", required_skills: "", required_roles: "", priority: "Medium", maximum_team_size: 5 };
const asText = (value) => Array.isArray(value) ? value.join(", ") : value || "";

export default function AdminProjectPlanner() {
  const [projects, setProjects] = useState([]); const [managers, setManagers] = useState([]); const [departments, setDepartments] = useState([]); const [form, setForm] = useState(null);
  const load = async () => { try { const [projectData, managerData, departmentData] = await Promise.all([getProjects(), getManagers(), getDepartments()]); setProjects(projectData.projects || []); setManagers(managerData.managers || []); setDepartments(departmentData.departments || []); } catch { toast.error("Could not load project data."); } };
  useEffect(() => { load(); }, []);
  const save = async (event) => { event.preventDefault(); try { const payload = { ...form, required_skills: form.required_skills.split(",").map((item) => item.trim()).filter(Boolean), required_roles: form.required_roles.split(",").map((item) => item.trim()).filter(Boolean) }; if (form.project_id) await updateProject(form.project_id, payload); else await createProject(payload); toast.success(`Project ${form.project_id ? "updated" : "created"}.`); setForm(null); load(); } catch (error) { toast.error(error.response?.data?.message || "Could not save project."); } };
  const edit = (project) => setForm({ ...project, manager_id: String(project.manager_id), department_id: String(project.department_id), start_date: project.start_date?.slice(0, 10), end_date: project.end_date?.slice(0, 10), required_skills: asText(project.required_skills), required_roles: asText(project.required_roles) });
  return <><div className="d-flex justify-content-between align-items-start gap-3 mb-4"><div><h1 className="admin-page-title">Project Planning</h1><p className="text-white-50 mb-0">Define the exact skills and roles needed before using Smart Allocation.</p></div><button className="btn btn-primary" onClick={() => setForm({ ...blank, manager_id: managers[0] ? String(managers[0].employee_id) : "", department_id: departments[0] ? String(departments[0].department_id) : "" })}><i className="bi bi-plus-lg me-2" />New project</button></div>
    {form && <form className="admin-panel mb-4" onSubmit={save}><div className="d-flex justify-content-between mb-3"><h5 className="mb-0">{form.project_id ? "Edit project" : "Create project"}</h5><button className="btn-close btn-close-white" type="button" onClick={() => setForm(null)} /></div><div className="row g-3"><Field label="Project name"><input required className="form-control" value={form.project_name} onChange={(event) => setForm({ ...form, project_name: event.target.value })} /></Field><Field label="Manager"><select required className="form-select" value={form.manager_id} onChange={(event) => setForm({ ...form, manager_id: event.target.value })}><option value="">Select manager</option>{managers.map((manager) => <option value={manager.employee_id} key={manager.employee_id}>{manager.full_name}</option>)}</select></Field><Field label="Department"><select required className="form-select" value={form.department_id} onChange={(event) => setForm({ ...form, department_id: event.target.value })}><option value="">Select department</option>{departments.map((department) => <option value={department.department_id} key={department.department_id}>{department.department_name}</option>)}</select></Field><Field label="Priority"><select className="form-select" value={form.priority} onChange={(event) => setForm({ ...form, priority: event.target.value })}>{["Low", "Medium", "High", "Critical"].map((item) => <option key={item}>{item}</option>)}</select></Field><Field label="Start date"><input required type="date" className="form-control" value={form.start_date} onChange={(event) => setForm({ ...form, start_date: event.target.value })} /></Field><Field label="End date"><input required type="date" className="form-control" value={form.end_date} onChange={(event) => setForm({ ...form, end_date: event.target.value })} /></Field><Field label="Maximum team size"><input required min="1" type="number" className="form-control" value={form.maximum_team_size} onChange={(event) => setForm({ ...form, maximum_team_size: event.target.value })} /></Field><Field label="Status"><select className="form-select" value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value })}>{["Active", "On Hold", "Completed"].map((item) => <option key={item}>{item}</option>)}</select></Field><Field label="Required skills (comma-separated)" wide><input className="form-control" value={form.required_skills} onChange={(event) => setForm({ ...form, required_skills: event.target.value })} placeholder="React, Node.js, PostgreSQL" /></Field><Field label="Required roles (comma-separated)" wide><input className="form-control" value={form.required_roles} onChange={(event) => setForm({ ...form, required_roles: event.target.value })} placeholder="Frontend Developer, Backend Developer" /></Field><Field label="Description" wide><textarea className="form-control" value={form.description || ""} onChange={(event) => setForm({ ...form, description: event.target.value })} /></Field><div><button className="btn btn-primary me-2">Save project</button><button type="button" className="btn btn-outline-light" onClick={() => setForm(null)}>Cancel</button></div></div></form>}
    <div className="admin-panel"><div className="table-responsive"><table className="table table-dark table-hover align-middle mb-0"><thead><tr><th>Project & staffing needs</th><th>Manager</th><th>Deadline</th><th>Team</th><th /></tr></thead><tbody>{projects.map((project) => <tr key={project.project_id}><td><strong>{project.project_name}</strong><small className="d-block text-primary">{asText(project.required_roles) || "No roles specified"}</small><small className="d-block text-white-50">{asText(project.required_skills) || "No skills specified"}</small></td><td>{project.manager}</td><td>{project.end_date?.slice(0, 10)}</td><td>{project.maximum_team_size || 5}</td><td className="text-nowrap"><button className="btn btn-sm btn-outline-light me-2" onClick={() => edit(project)}><i className="bi bi-pencil" /></button><button className="btn btn-sm btn-outline-danger" onClick={async () => { if (!window.confirm(`Delete ${project.project_name}?`)) return; try { await deleteProject(project.project_id); toast.success("Project deleted."); load(); } catch (error) { toast.error(error.response?.data?.message || "Could not delete project."); } }}><i className="bi bi-trash" /></button></td></tr>)}{!projects.length && <tr><td colSpan="5" className="text-center text-white-50 py-5">No projects yet.</td></tr>}</tbody></table></div></div></>;
}
function Field({ label, children, wide = false }) { return <div className={wide ? "col-12" : "col-md-6"}><label className="form-label">{label}</label>{children}</div>; }
